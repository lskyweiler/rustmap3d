[package]
name = "rustmap3d"
authors = [
    "Lucas Weiler <lucas.l.weiler@leidos.com>",
    "Andrew Molnar <andrew.b.molnar@leidos.com>",
]
description = "Python geodetic coordinate conversion library written in rust"
keywords = ["geodetic", "engineering", "scientific", "gis"]
license-file = "LICENSE"
homepage = "https://github.com/lskyweiler/rustmap3d"
repository = "https://github.com/lskyweiler/rustmap3d"
readme = "README.md"
version = "0.6.0"
edition.workspace = true

[workspace]
members = ["crates/*"]

[workspace.package]
edition = "2021"
version = "0.6.0"

[workspace.dependencies]
map3d = { path = "crates/map3d", features = ["pyo3"] }
pymap3d = { path = "crates/pymap3d" }

bevy = "~0.18"

either = "1.15"
glam = "~0.30"
pyo3 = { version = "~0.27", features = [
    "either",
    "extension-module",
    "generate-import-lib",
] }
pyo3-stub-gen = { version = "~0.19", default-features = false, features = [
    "either",
] }

[lib]
# rlib is required to generate py-stubs
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
map3d.workspace = true
pymap3d.workspace = true
pyo3.workspace = true
glam.workspace = true

# bin/stub_gen deps
env_logger = "~0.11"
pyo3-stub-gen.workspace = true

[profile.release]
# https://github.com/johnthagen/min-sized-rust
strip = true # Automatically strip symbols from the binary.

[features]
# if you're building from here, you always want python functionality
default = ["pyo3", "serde", "map3d/pydantic-serde"]
# generate python functions in geo_objects
pyo3 = ["map3d/pyo3"]
# generates generic bevy derives (reflect, serde)
serde = ["map3d/serde"]
bevy = ["map3d/bevy", "serde"]
# generates bevy ref structs
py-bevy = ["map3d/py-bevy", "bevy", "pyo3"]
